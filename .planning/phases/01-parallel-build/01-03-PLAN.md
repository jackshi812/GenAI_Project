---
phase: 01-parallel-build
plan: 03
type: execute
wave: 1
depends_on: []
owner: Jack
autonomous: true
files_modified:
  - contracts.py
  - fixtures.json
  - serper_fixtures.json
  - record_serper_fixtures.py
  - .env.example
  - .gitignore
  - requirements.txt
  - voice/stt.py
  - voice/tts.py
  - voice/smoke.py
  - voice/CLAUDE.md
  - voice/AGENTS.md
  - app/main.py
  - app/CLAUDE.md
  - app/AGENTS.md
requirements:
  - DOC-04
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "Austin and Ginger import product shapes from one file instead of each inventing their own"
    - "A price arriving as a string instead of a number raises immediately rather than breaking a budget filter at integration"
    - "web.search returns real-shaped results before anyone holds a Serper key"
    - "A user clicks record, clicks stop, and never touches the keyboard again"
    - "The transcript appears on screen once transcription finishes"
    - "The spoken answer plays automatically and can be replayed with a Play control"
    - "Spoken answers run at or under fifteen seconds, verified on all three canonical answers"
    - "The full result screen renders from fixtures.json with no graph running"
    - "Fields that disagree between 2020 and live data are visibly marked on the affected row"
    - "Private document IDs and live source URLs are visually distinct"
    - "Nodes light up as the graph executes"
  artifacts:
    - contracts.py
    - fixtures.json
    - serper_fixtures.json
    - .env.example
    - .gitignore
    - requirements.txt
    - voice/stt.py
    - voice/tts.py
    - app/main.py
  key_links:
    - "contracts.py -> imported by graph/ and mcp_server/ -> one definition of a product row, not three (D-10)"
    - "serper_fixtures.json -> Austin's web_search replay path -> he is unblocked before a key exists (D-08)"
    - "fixtures.json -> Ginger's tools_stub.py and Jack's app -> both layers run standalone"
    - "st.audio_input -> voice/stt.py transcribe -> voice/tts.py synthesize -> st.audio autoplay -> hands-free loop (D-15)"
    - "AssistantResult.products[].conflicts -> red field markers on the affected row (UI-02)"
---

<objective>
**Owner: Jack.** This plan is yours. Austin owns `catalog/` and `mcp_server/`; Ginger owns `graph/` and `prompts/`. You own `voice/`, `app/`, and everything at the repository root — including `contracts.py`, which only you write.

Two jobs. First, the shared scaffolding: the interface contract, the mock fixtures, the environment template, and the recorded Serper responses. That is roughly a day of work that blocks nobody today but that both teammates import from, so it goes first and gets committed immediately. Second, the user-facing layer: browser microphone capture, Whisper transcription, OpenAI speech synthesis with a fifteen-second cap, and the Streamlit interface.

Purpose: your layer carries 10 UI/UX points and the visible half of the 28 Functionality points. It is also the entire demo surface — everything the grader actually sees.

Output: a repository that a teammate can clone and configure in five minutes, and an interface that renders a complete result screen from `fixtures.json` with no graph and no MCP server in existence.
</objective>

<execution_context>
Executed by Jack in his own coding agent (Claude Code / Codex / Cursor), on his own machine, over roughly Aug 10–16. Not run by `/gsd-execute-phase`.

Commit directly to `main` after each task (D-11). **Task 1 and Task 2 get committed and announced the same day you write them** — the other two are importing from them.
</execution_context>

<context>
@Instructions.md
@.planning/PROJECT.md
@.planning/phases/01-parallel-build/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared scaffolding — contracts, fixtures, environment, dependencies</name>

  <read_first>
    - `Instructions.md` §80–88 — the `web.search` return schema, verbatim
    - `Instructions.md` §99–107 — the `rag.search` return schema, verbatim
    - `Instructions.md` §143 — citations and data lineage, including private document IDs and live links
    - `Instructions.md` §221–228 — the documentation deliverables, including `.env.example`
    - `.planning/phases/01-parallel-build/01-CONTEXT.md` — decisions D-02, D-03, D-09, D-10, D-12, D-14
    - The CSV header row of `dataset/marketing_sample_for_amazon_com-ecommerce__20200101_20200131__10k_data.csv`, so your fixture products are real
  </read_first>

  <files>contracts.py, fixtures.json, .env.example, .gitignore, requirements.txt, voice/CLAUDE.md, voice/AGENTS.md, app/CLAUDE.md, app/AGENTS.md</files>

  <action>
**Do this first, today, and push it before you write another line.** It is about two hours and it is what makes the other two tracks independent.

**`contracts.py` at the repository root — pydantic models, the single source of truth (D-10).** Austin and Ginger import these; they do not redefine them. Runtime validation catches exactly the bug class this structure exists to prevent: a price arriving as the string `"17.49"` instead of the number `17.49` raises here, on day two, instead of silently breaking an under-twenty-dollars filter during integration on August 17.

Define eight models, no more:

`RagResult` — the seven keys spec §99–107 mandates: `sku` (str), `title` (str), `price` (float or str — the spec types it "number or string", and the string case carries the rows whose price could not be parsed), `rating` (float or None, **always None** because the dataset has no ratings anywhere), `brand` (str or None), `ingredients` (str or None, always None), `doc_id` (str). Plus additive fields the interface needs: `image_url`, `product_url`, `category`, `price_low`, `price_high`, `similarity`, `budget_fit` (one of `within`, `partial`, `unknown`). Add a comment marking which keys are spec-mandated and which are additive, so a grader reading this file can see the schema was honored rather than reinvented.

**Where the type safety actually comes from.** `price` is deliberately `float | str`, so `"17.49"` is *valid* under it — the union cannot tell an intentionally unparseable raw price from a mistakenly stringified number. That is unavoidable; the spec requires the union. So put the strictness where it can exist: type `price_low` and `price_high` as `float | None` with `model_config = ConfigDict(strict=True)` on the model, which makes a stringified number raise rather than silently coerce. Those two fields are what every budget filter and every price conflict actually reads, so that is where a type error would do real damage. Add a validator on `price` that rejects a string which parses cleanly as a number — if it is numeric, it should have been a number.

`WebResult` — the five keys spec §80–88 mandates: `title`, `url`, `snippet`, `price` (optional), `availability` (optional). Plus `rating` (float or None) as an additive sixth field. Mark it as an addition in a comment: the private catalog has no ratings in any row, so Serper's shopping results are the only source of ratings in the entire system, and dropping the field would throw away the one thing live search uniquely provides.

`MatchInfo` — `similarity` (float), `verdict` (one of `same`, `different`, `unsure`), `reason` (str). Shown on screen per product (D-14) so the matching is inspectable and a wrong match has an honest explanation during the demo.

`Conflict` — `field` (str), `private_value`, `live_value`, `note` (str). **A conflict is a genuine disagreement between two known values — in practice, price.** Rating and availability never appear here: the catalog has no rating in any row, so a live rating has nothing to disagree with. It is live-only data, not a problem, and rendering it as one would tell a grader the system found a fault where none exists. Per-field provenance (D-02) is carried by the two-column structure — `private` is what 2020 knew, `live` is what the web knows — not by this list.

`ComparisonProduct` — `private` (a `RagResult`), `live` (a `WebResult` or None), `conflicts` (list of `Conflict`), `match` (a `MatchInfo` or None). Two side-by-side sub-objects plus a precomputed conflicts list (D-09). `live` being None is a first-class state, not an error: many 2020 listings are genuinely delisted, and the interface shows that honestly rather than hiding the row (D-03).

`StepEvent` — `node` (str), `tool` (str or None), `started_at` (str), `duration_ms` (int or None), `status` (str), `detail` (str).

`Citation` — `kind` (`private` or `live`), `label` (str — a doc_id or a domain), `url` (str or None).

`AssistantResult` — `transcript` (str), `plan` (str or None), `answer_text` (str), `products` (list of `ComparisonProduct`), `steps` (list of `StepEvent`), `citations` (list of `Citation`).

**`fixtures.json` at the repository root.** Three top-level keys. `rag_results` holds four or five product dicts pulled from **real rows of the CSV** — real titles, real prices, real image URLs — so the shapes are genuine rather than imagined. `web_results` maps a query string to a list of `WebResult` dicts; Task 2 replaces these with recorded Serper data. `canonical_transcripts` holds three spoken-language transcripts covering the budget, currency, and conflict cases.

Austin commits a **provisional** `catalog/canonical_queries.json` on day one, as part of his Task 1, specifically so you are not guessing. Use his products. He finalizes the same file in his Task 4 once retrieval works; if a pick changes he tells the group chat and you re-record one fixture group, which is ten minutes.

If the file genuinely is not there yet, ping him before picking your own — an hour of waiting is cheaper than recording ten Serper responses for products nobody ends up demoing.

**`.env.example` (DOC-04).** Every variable the system reads, with empty values for secrets and real defaults for everything else. Secrets, left empty: `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `SERPER_API_KEY`. Non-secrets, with defaults: `LLM_PROVIDER=anthropic`, `LLM_MODEL=claude-opus-5`, `WHISPER_MODEL=whisper-1`, `TTS_MODEL=gpt-4o-mini-tts`, `TTS_VOICE=alloy`, `CHROMA_PATH=catalog/chroma`, `WEB_SEARCH_CACHE_TTL=180`, `WEB_SEARCH_MIN_INTERVAL_S=1.0`, `WEB_SEARCH_MAX_CALLS=50`. Add a one-line comment above each secret naming where to obtain it. **No real secret ever goes in this file** — it is committed, and spec §236 says never log or commit secrets.

Note in a comment that `LLM_MODEL` accepts a cheaper model such as `claude-sonnet-5` if API spend becomes a concern during the ten days. Swapping it is exactly the swappability spec §149 requires, and demonstrating the swap on camera is worth points.

**`.gitignore`.** Cover: `.env`, `venv/`, `.venv/`, `__pycache__/`, `*.pyc`, `catalog/chroma/`, `mcp_server/logs/`, `*.mp3`, `*.wav`, `.DS_Store`. The Chroma directory and the MCP log directory are Austin's generated output — he asked for these lines and does not edit this file himself.

**`requirements.txt`.** One pinned file at the repository root, with a virtual environment alongside it. List: `streamlit`, `langgraph`, `langchain-core`, `langchain-anthropic`, `langchain-openai`, `chromadb`, `pandas`, `openai`, `mcp`, `requests`, `pydantic`, `python-dotenv`. Install them, then pin each line with the exact version `pip` resolved on your machine — read them off `pip freeze`. Do not invent version numbers; pin what actually installed.

Before the first install, spend two minutes confirming each package name on pypi.org. All twelve are major first-party or well-known projects, and a typo'd name is the one supply-chain risk a project this size actually has.

This is the one file all three of you might need to touch. Listing every dependency for every layer up front means Austin and Ginger should never have to — if one of them does add a line, that is the single expected merge conflict of the phase (D-11).

**Folder briefs (D-12).** Write `voice/CLAUDE.md` and `app/CLAUDE.md`, roughly fifteen lines each, saying the folder is Jack's, what it holds, that `contracts.py` is the single source of truth and its models are imported rather than redefined, and that the interface renders whatever the graph hands it and contains no business logic (D-09). Copy each verbatim to `voice/AGENTS.md` and `app/AGENTS.md` — Claude Code auto-loads the first, Codex and Cursor auto-load the second, so a teammate opening any agent gets the brief with no setup.

**Commit and announce this task the moment it passes.** Say so in the group chat. Austin and Ginger are waiting on nothing today, but they will be tomorrow.
  </action>

  <acceptance_criteria>
    - `python -c "import contracts; print([m for m in dir(contracts) if m[0].isupper()])"` lists all eight model names
    - `python -c "import json,contracts; d=json.load(open('fixtures.json')); [contracts.RagResult(**r) for r in d['rag_results']]"` validates every fixture product without raising
    - Round-tripping every entry in `fixtures.json` through its model (`RagResult(**d)` for each `rag_results` entry, `WebResult(**d)` for each `web_results` entry) raises nothing
    - Setting a fixture's `price_low` to the string `"17.49"` raises a pydantic `ValidationError` under strict mode — this is the coercion bug the contract exists to catch
    - Setting a fixture's `price` to the string `"17.49"` also raises, via the numeric-string validator, while leaving a genuinely unparseable value like `"Total price:"` valid
    - Removing a required key from any fixture entry raises a `ValidationError`
    - `.env.example` lists all twelve variables, with all three secret values empty
    - `grep -nE "sk-|AIza|[A-Za-z0-9]{32}" .env.example` returns nothing
    - `.gitignore` contains `.env`, `catalog/chroma/`, and `mcp_server/logs/`
    - `pip install -r requirements.txt` succeeds in a fresh virtual environment and every line carries an exact `==` pin
    - `contracts.py`, `fixtures.json`, `.env.example`, `.gitignore`, and `requirements.txt` are all committed and pushed to `main`
    - All four folder brief files exist and each `AGENTS.md` is byte-identical to its `CLAUDE.md`
  </acceptance_criteria>

  <done>The shared contract, fixtures, environment template, and dependency list exist and are committed. Austin and Ginger can import from them.</done>
</task>

<task type="auto">
  <name>Task 2: Serper key and recorded shopping fixtures (D-08)</name>

  <read_first>
    - `Instructions.md` §80–91 — the `web.search` return schema plus the rate-limit and cache requirements Austin implements against these fixtures
    - `.planning/phases/01-parallel-build/01-CONTEXT.md` — decisions D-05 (shopping endpoint) and D-08 (fixture replay)
    - `.planning/ROADMAP.md` — the recorded risk about network calls in the live demo's critical path
    - `catalog/canonical_queries.json` if Austin has committed it
    - Serper's shopping endpoint documentation at serper.dev
  </read_first>

  <files>record_serper_fixtures.py, serper_fixtures.json, fixtures.json</files>

  <action>
Get a Serper key at serper.dev — the free tier is 2,500 credits, which is far more than this project will consume. Put it in your local `.env` as `SERPER_API_KEY`. Never commit `.env`; Task 1's `.gitignore` already covers it.

Create `record_serper_fixtures.py` at the repository root. It holds a list of roughly ten query strings, POSTs each to `https://google.serper.dev/shopping` with the `X-API-KEY` header and a JSON body of `q` and `num`, and writes the **raw, unmodified** responses to `serper_fixtures.json` keyed by query. Raw, not normalized — Austin's `web_search` normalizes them through the same code path a live call takes, which is what makes his fixture mode a genuine rehearsal of the live path rather than a parallel one.

The ten queries: the three canonical products from Austin's `catalog/canonical_queries.json`, plus five to seven more drawn from the catalog, chosen so at least a few are still-sold, well-known products that shopping search will actually find. Sleep one second between calls so you do not trip Serper's rate limit while recording.

**This file lives at the repository root, not inside `mcp_server/`.** Austin's folder is his; he reads this file, you write it. That keeps the ownership boundary clean with no coordination cost.

Once recorded, curate the interesting ones into `fixtures.json`'s `web_results` key, normalized to the `WebResult` shape from `contracts.py` — that is what Ginger's stub returns. Make sure at least one product's live price genuinely differs from its 2020 catalog price by enough to be visible on screen. That single row is the demo's centerpiece: reconciliation is the project's core value and ten rubric points, and if the fixture shows no disagreement there is nothing to demonstrate.

Commit `serper_fixtures.json` and `record_serper_fixtures.py`, then tell Austin. His `web.search` replays this file whenever `SERPER_API_KEY` is unset, which unblocks him immediately and makes his smoke output deterministic.

These recordings do double duty on purpose: they unblock Austin before a key exists, and they are the offline fallback if the network dies during the live demonstration — which is a recorded project risk with no other mitigation.
  </action>

  <acceptance_criteria>
    - `python record_serper_fixtures.py` completes with a valid key and writes `serper_fixtures.json`
    - `serper_fixtures.json` parses as JSON and holds at least eight query keys, each mapping to a response containing a non-empty `shopping` array
    - At least three entries include both a `price` and a `rating` field — ratings are the one thing the private catalog cannot supply
    - `fixtures.json`'s `web_results` key holds normalized `WebResult`-shaped entries that validate against `contracts.WebResult`
    - At least one fixture product shows a live price differing from its 2020 catalog price by more than 15%
    - `git status` shows `.env` untracked; `grep -rn "$SERPER_API_KEY" serper_fixtures.json record_serper_fixtures.py` returns nothing
    - Both files are committed and Austin has been told
  </acceptance_criteria>

  <done>Real Serper shopping responses are recorded and committed. Austin's `web.search` works with no key, and the live demo has an offline fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Speech to text</name>

  <read_first>
    - `Instructions.md` §41–51 — the ASR section; §47 permits fragment-based recording, which is what you are building
    - `Instructions.md` §24 and §235 — fragment-based processing is acceptable and encouraged; record then transcribe
    - `.planning/REQUIREMENTS.md` — VOICE-01 and VOICE-02
    - OpenAI's audio transcription API reference
  </read_first>

  <files>voice/stt.py, voice/smoke.py</files>

  <action>
Create `voice/stt.py` with one public function:

`transcribe(audio_bytes, filename="audio.wav") -> str`

It constructs an OpenAI client, calls `client.audio.transcriptions.create` with `model=os.getenv("WHISPER_MODEL", "whisper-1")` and the audio passed as a `(filename, audio_bytes)` tuple, and returns the transcript text. Roughly ten lines.

Fragment-based, not streaming. Spec §24 calls fragment-based processing acceptable and encouraged, §235 says implement it first, and streaming buys zero rubric points while adding real risk on a ten-day timeline.

Return the plain transcript string. Spec §49 mentions returning timestamps when available — the graph does not consume them and nothing in the rubric rewards them, so skip it.

Handle the empty-recording case: if the audio is under roughly 1 KB, return an empty string rather than calling the API. A user who clicks record and immediately clicks stop should see nothing happen, not an API error.

Create `voice/smoke.py` with an `if __name__ == "__main__"` block that reads a WAV file path from `sys.argv`, transcribes it, and prints the transcript. Record yourself saying one of the canonical transcripts on your phone, save it into the repository root as a scratch file, and use that. This proves the transcription path works before any Streamlit code exists.

Browser microphone capture happens in Task 5 through Streamlit's built-in `st.audio_input`, which hands you exactly the bytes this function takes.
  </action>

  <acceptance_criteria>
    - `python -m voice.smoke path/to/recording.wav` prints a transcript that recognizably matches what you said
    - Speaking one of the canonical transcripts produces a transcript close enough that Ginger's router would extract the same budget from it
    - Passing a near-empty file returns an empty string without an API error
    - `grep -rn "OPENAI_API_KEY" voice/stt.py` shows the key read via `os.getenv` only, never printed and never hardcoded
  </acceptance_criteria>

  <done>Recorded audio transcribes to text through the Whisper API.</done>
</task>

<task type="auto">
  <name>Task 4: Text to speech with the fifteen-second cap</name>

  <read_first>
    - `Instructions.md` §119–129 — the TTS section; §127 states the summary must be no longer than 15 seconds and must align with the citations shown on screen
    - `Instructions.md` §142 — a Play TTS button is required
    - `.planning/REQUIREMENTS.md` — VOICE-03 and VOICE-04
    - OpenAI's speech synthesis API reference
  </read_first>

  <files>voice/tts.py</files>

  <action>
Create `voice/tts.py` with two functions.

`cap_for_speech(text, max_words=30) -> str` truncates to at most `max_words` words, cutting at the last sentence boundary within the limit when one exists so the answer does not end mid-phrase.

**Thirty, not forty.** Speech runs around 150 words per minute, so forty words is roughly sixteen seconds and spec §127's ceiling is fifteen — forty words fails the requirement outright. Thirty lands near twelve seconds and leaves headroom, which is needed because a word count is not a duration guarantee: product names, dollar amounts and long brand strings all synthesize slower than ordinary prose. Ginger's answerer prompt targets 30 words; this is the enforcement that makes VOICE-04 true regardless of what the model produces on the day.

`synthesize(text) -> bytes` calls `client.audio.speech.create` with `model=os.getenv("TTS_MODEL", "gpt-4o-mini-tts")`, `voice=os.getenv("TTS_VOICE", "alloy")`, and the capped input, returning MP3 bytes. Call `cap_for_speech` inside `synthesize` so nothing can bypass it.

Fragment-based: generate the complete MP3, then play it. Spec §125 permits exactly this.

Add an `if __name__ == "__main__"` block that synthesizes **each of the three canonical answers** — realistic 30-word answers naming a product, a price and a rating — writing `out_1.mp3`, `out_2.mp3`, `out_3.mp3` and printing each word count and byte size.

Do not add a duration-measurement dependency. Play all three with a stopwatch and confirm each is at or under 15.0 seconds. Three files, not one: the fifteen-second ceiling has to hold for whatever you actually demo, and the conflict answer is the longest of the three because it names two prices.
  </action>

  <acceptance_criteria>
    - `python -m voice.tts` writes `out_1.mp3`, `out_2.mp3` and `out_3.mp3`, each printing a word count at or under 30
    - Each of the three, timed by stopwatch, is at or under 15.0 seconds — all three, not just the shortest
    - `cap_for_speech` applied to a 200-word paragraph returns at most 30 words and does not end mid-sentence
    - `cap_for_speech` applied to a 20-word answer returns it unchanged
    - Passing an over-length string directly to `synthesize` still yields audio at or under 15.0 seconds, proving the cap cannot be bypassed
  </acceptance_criteria>

  <done>Answer text synthesizes to playable MP3 audio, verified at or under the fifteen-second ceiling on all three canonical answers.</done>
</task>

<task type="auto">
  <name>Task 5: Streamlit app — two-column layout, hands-free flow, comparison table</name>

  <read_first>
    - `Instructions.md` §131–145 — the User Interface section in full; §138 record-then-send microphone capture, §139 transcript display, §141 comparison table, §142 the Play TTS button
    - `Instructions.md` §169 — the screen should show a top-three comparison table with price, rating, and ingredients
    - `.planning/phases/01-parallel-build/01-CONTEXT.md` — decisions D-13 (two-column layout), D-14 (product images, match confidence), D-15 (one click then hands-free)
    - `contracts.py` — `AssistantResult` and `ComparisonProduct`, the shapes you render
    - `fixtures.json` — the data you render from
    - Streamlit's `st.audio_input`, `st.audio`, and `st.columns` documentation
  </read_first>

  <files>app/main.py</files>

  <action>
Create `app/main.py`. Streamlit over React — React costs an entire track the ten-day timeline cannot absorb, and the interface is worth 10 points that Streamlit earns.

**Layout (D-13).** `st.set_page_config(layout="wide")`, then `left, right = st.columns([1, 1.4])`. Conversation lives on the left: microphone, transcript, spoken answer text, audio player. Evidence lives on the right: comparison table, then Task 6's step log, citations, and agent graph. Both halves stay visible at once, which is what lets evidence appear on screen while the presenter is still talking — the thing that actually demonstrates reconciliation inside a seven-minute limit.

**Microphone (VOICE-01, D-15).** Use Streamlit's built-in `st.audio_input("Ask for a product")`. It renders a record button, a stop button, and returns the recording as an uploaded file. No extra dependency, and its record-then-stop interaction is exactly the one D-15 specifies. Do not install `streamlit-mic-recorder`.

**Hands-free flow (D-15).** When `st.audio_input` returns a new recording, everything else follows automatically with no further clicks: call `voice.stt.transcribe`, display the transcript (VOICE-02), fetch the result, synthesize the answer, and play it. Guard on a hash of the audio bytes held in `st.session_state` so a Streamlit rerun does not re-transcribe the same recording and burn API calls.

**Audio playback (VOICE-03, spec §142).** `st.audio(mp3_bytes, format="audio/mp3", autoplay=True)`. The widget renders a play control, satisfying the required Play TTS button, and `autoplay=True` closes the hands-free loop. Browser autoplay policy may block the first playback until the user has interacted with the page — that risk is accepted rather than designed around (D-15), and the visible Play control is the fallback. Note it in a code comment so it does not read as a bug during the demo.

**Data source.** Write `load_result() -> AssistantResult` that reads `fixtures.json` and assembles a complete `AssistantResult`, validated through the pydantic model. **Call it from exactly one place in the app.** On August 18, Phase 2 replaces that single call with Ginger's `run_graph`. Do not add a feature flag, an adapter, or a data-source abstraction — one call site is the whole design.

**Comparison table (UI-01).** Top three products with price, rating, and product image. Do not fight `st.dataframe` — per-cell conflict marking in Task 6 is painful there. Instead render three `st.container(border=True)` blocks, each split into `st.columns([1, 2, 2])`: image on the left via `st.image(product.private.image_url, width=110)`; the 2020 catalog column in the middle; the live column on the right. Header each of the two data columns so the two-source structure is unmistakable — something like "Private catalog (2020)" and "Live web".

Three fields need honest empty states rather than silence:

- **Ingredients.** Spec §169 names it in the comparison table, and the column is empty in all 10,002 rows. Render the row with the caption `Ingredients: not available in source data`. Showing the field as explicitly unavailable answers the spec; omitting it silently looks like it was forgotten, and a grader cannot tell the difference between a gap you knew about and one you missed.
- **Rating on the catalog side.** Same treatment — `no rating in 2020 catalog` — which makes the live rating beside it visibly the only source.
- **Budget fit.** When `private.budget_fit == "partial"`, print `starting at ${price_low} — some variants exceed budget` under the price. Austin already sorts these below products wholly within budget; this is the line that stops a $15.99–$44.99 product reading as a clean "under $20" recommendation on camera.

If Austin's `canonical_queries.json` notes flag a pack-size difference between compared products, print `different size — not directly comparable` on that row. That is how this project satisfies spec §202's fair-comparison requirement: by disclosure, not by building a unit parser.

Per-row fields: title, price, rating, availability, brand. Two cases need care and both are honest rather than awkward.

**The private rating is always null**, in every product, because the dataset has no ratings in any row. Render it as an em dash with a small caption reading that the 2020 catalog carries no ratings. Do not blank it silently and never fabricate one — showing the absence is the clearest possible demonstration of per-field provenance (D-02) and it feeds spec §143's data-lineage requirement directly.

**A product may have no live match at all** (D-03). Render the live column in an explicit empty state — something like "no live match found" with a short caption noting the 2020 listing may be delisted. Never drop the row and never backfill it to make the screen look complete. Many 2020 listings genuinely no longer exist, and that is a finding worth showing.

Below each row, add a caption showing match confidence (D-14): the similarity as a percentage plus the LLM verdict and its one-sentence reason, drawn from `product.match`. Displaying the match rather than hiding it makes the matching inspectable and gives you an honest answer when a match is wrong on camera.

Add an `if __name__ == "__main__"` guard is unnecessary in Streamlit; the run command is `streamlit run app/main.py`.
  </action>

  <acceptance_criteria>
    - `streamlit run app/main.py` opens a two-column page with the conversation on the left and evidence on the right
    - The page renders a complete comparison table from `fixtures.json` alone, with no graph and no MCP server running
    - Three product rows appear, each showing a real product image loaded from the dataset's image URL
    - The private rating column shows an em dash with the "no ratings in the 2020 catalog" caption, on every row
    - Every row shows `Ingredients: not available in source data` rather than omitting the field (spec §169)
    - A fixture product with `budget_fit: "partial"` renders the "starting at ... some variants exceed budget" line under its price
    - No conflict marker appears on a live-only rating or availability value
    - A fixture product with `live` set to null renders an explicit "no live match" state and is still visible
    - Each row shows a match confidence percentage and an LLM verdict
    - Clicking record, speaking a canonical query, and clicking stop causes the transcript to appear and audio to play with no further clicks
    - The audio widget shows a play control that replays the answer
    - Reloading the page and rerunning does not re-transcribe the previous recording
    - `grep -n "load_result()" app/main.py` shows exactly one call site
  </acceptance_criteria>

  <done>The app renders a full two-column screen from fixtures, captures speech hands-free, displays the transcript, and plays the spoken answer.</done>
</task>

<task type="auto">
  <name>Task 6: Evidence column — conflict badges, step log, citations, live agent graph</name>

  <read_first>
    - `Instructions.md` §140 — display the agent step log
    - `Instructions.md` §143 — show citations and data lineage, including private document IDs and live links
    - `.planning/phases/01-parallel-build/01-CONTEXT.md` — decisions D-02 (per-field provenance), D-14 (all four interface features, agent graph included)
    - `.planning/REQUIREMENTS.md` — UI-02, UI-03, UI-04
    - `contracts.py` — `Conflict`, `StepEvent`, `Citation`
    - Streamlit's `st.graphviz_chart` documentation
  </read_first>

  <files>app/main.py</files>

  <action>
Extend the right-hand column with the remaining three interface features plus the agent graph.

**Conflict badges (UI-02, D-02).** Ginger hands you a precomputed `conflicts` list per product (D-09), so there is no comparison logic to write here — walk the list and mark the named fields.

**Only genuine disagreements arrive in that list, and in practice that means price.** Ratings and availability are live-only fields, not conflicts: the 2020 catalog has no rating in any row, so there is nothing for a live rating to disagree with. Render those as ordinary values in the live column. If you mark a live-only rating red with a warning glyph, you are telling a grader the system detected a fault where none exists — and that is the opposite of what the reconciliation is supposed to demonstrate. Provenance comes from the two columns being labelled `Catalog (2020)` and `Live`, not from colouring things.

Build a set of conflicting field names per product, and when rendering a field in the comparison table, wrap conflicting values in Streamlit's red markdown colour and prefix them with a warning glyph. Under the row, print one line per conflict showing the field, the 2020 value, the live value, and the note. The visible marker goes on the affected row and on the affected field, not in a separate panel — a badge somewhere else on the page does not make the disagreement legible.

**Step log (UI-03).** Render the `steps` list as a compact table with columns for node, tool, duration in milliseconds, status, and detail. Put it in an expander that starts expanded, with a header showing the node count and total elapsed time. Spec §140 asks for it and it is where the multi-agent structure becomes visible to a grader who cannot read the code.

**Citations panel (UI-04).** Two visually distinct groups. Private citations render the `doc_id` in monospace with a "private catalog" label and a small badge; live citations render as clickable links with the domain shown and a "live" badge. Use different colours or different icons — the requirement is that a viewer can tell at a glance which claims came from the private corpus and which came from the web, which is spec §143's data-lineage ask.

**Live agent graph (D-14).** Four nodes — Router, Planner, Retriever, Answerer — drawn with `st.graphviz_chart`, coloured by execution state: grey for pending, amber for running, green for complete. Build a DOT string from the current `steps` list: a node named in `steps` with a complete status is green, the most recent one is amber, the rest are grey. No extra dependency — a DOT string is just text and Streamlit renders it.

**This must repaint as the run proceeds, not once at the end.** Ginger's `run_graph` is a generator yielding a state snapshot after each node completes, precisely so this is possible. Render the graph into an `st.empty()` placeholder and rewrite it on each yield. During Phase 1 you are rendering from static fixtures, so simulate it: iterate the fixture `steps` list with a short sleep between entries and repaint each time. That proves the mechanism works, and on August 18 the fixture loop is replaced by the real generator with no other change.

Keep the visual order in the right-hand column: comparison table, conflicts, agent graph, step log, citations. That is roughly descending order of what a grader looks at first.
  </action>

  <acceptance_criteria>
    - A product with a price conflict shows the conflicting price in red with a warning marker, on that product's own row
    - Below that row, a line names the field, the 2020 value, the live value, and the note
    - A product with no conflicts shows no red marking anywhere
    - The step log renders every `StepEvent` with node, tool, duration and status, and the header shows the node count and total elapsed time
    - The citations panel shows private `doc_id` entries and live URL entries in visually distinct groups, and every live URL is clickable
    - The agent graph renders four nodes and, on load, they turn green one at a time rather than all at once
    - The whole right-hand column renders from `fixtures.json` alone with no graph running
    - A screenshot of the full page shows all four features at once: images and prices in the table, a red conflict marker, the step log, and the citations panel
  </acceptance_criteria>

  <done>The evidence column shows per-field conflicts on the affected rows, a live step log, distinguishable citations, and an agent graph whose nodes light up as execution proceeds.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
**Repository root — Jack writes these; Austin and Ginger only read them**

| Path | Contents |
|---|---|
| `contracts.py` | `RagResult`, `WebResult`, `MatchInfo`, `Conflict`, `ComparisonProduct`, `StepEvent`, `Citation`, `AssistantResult` |
| `fixtures.json` | `rag_results`, `web_results`, `canonical_transcripts` |
| `serper_fixtures.json` | Raw recorded Serper shopping responses keyed by query (D-08) |
| `record_serper_fixtures.py` | One-shot recording script |
| `.env.example` | All twelve variables; secrets empty (DOC-04) |
| `.gitignore` | `.env`, `venv/`, `__pycache__/`, `catalog/chroma/`, `mcp_server/logs/`, audio, `.DS_Store` |
| `requirements.txt` | Twelve pinned dependencies covering all three layers |

**`voice/` and `app/`**

| Path | Contents |
|---|---|
| `voice/stt.py` | `transcribe(audio_bytes, filename)` |
| `voice/tts.py` | `cap_for_speech(text, max_words)`, `synthesize(text)` |
| `voice/smoke.py` | Command-line transcription check |
| `app/main.py` | Streamlit app: two-column layout, `load_result()`, comparison table, conflict badges, step log, citations panel, agent graph |
| `voice/CLAUDE.md`, `voice/AGENTS.md`, `app/CLAUDE.md`, `app/AGENTS.md` | Folder briefs, identical pairs (D-12) |

**Environment variables declared:** `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `SERPER_API_KEY`, `LLM_PROVIDER`, `LLM_MODEL`, `WHISPER_MODEL`, `TTS_MODEL`, `TTS_VOICE`, `CHROMA_PATH`, `WEB_SEARCH_CACHE_TTL`, `WEB_SEARCH_MIN_INTERVAL_S`, `WEB_SEARCH_MAX_CALLS`.

**Run command:** `streamlit run app/main.py`

**Phase 2 seam:** the single `load_result()` call site in `app/main.py` is replaced by Ginger's `run_graph` generator on August 18. Nothing else in `app/` changes at that seam.
</artifacts_this_phase_produces>

<threat_model>
Scoped to a single-session local demo application with no authentication, no persistence, and no multi-user surface. Every item traces to spec line 236.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser microphone → `voice/stt.py` | User-supplied audio crosses into an external API call |
| `.env` → committed repository | The one place a secret could leak into git history |
| PyPI → `requirements.txt` | Package names are typed by hand |
| Live result URLs → rendered links | Third-party URLs become clickable elements on the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Information Disclosure | API keys | high | mitigate | `.gitignore` covers `.env`; `.env.example` carries names with empty values only (DOC-04, spec §236); keys read solely via `os.getenv` and never rendered on the page |
| T-03-SC | Tampering | `pip install -r requirements.txt` | high | mitigate | All twelve packages are major first-party or well-known projects; verify each name on pypi.org before the first install (typosquat check) and pin every line with an exact `==` version read from `pip freeze` |
| T-03-02 | Information Disclosure | Recorded audio | low | accept | Audio is sent to OpenAI for transcription and never written to disk; single-session demo with no persistence |
| T-03-03 | Tampering | Live result URLs rendered in the citations panel | medium | mitigate | Only URLs that passed Austin's retailer allowlist in `mcp_server/web_search.py` reach the interface (D-07); render the domain as the visible label so the destination is legible before clicking |
| T-03-04 | Spoofing | Fabricated ratings on screen | medium | mitigate | The private rating column always renders an em dash with an explicit "no ratings in the 2020 catalog" caption; the interface has no code path that can invent a rating |
| T-03-05 | Denial of Service | Repeated transcription on Streamlit rerun | low | mitigate | Guard on an audio-bytes hash held in `st.session_state` so a rerun cannot re-call the transcription API |
</threat_model>

<verification>
Run in order after all six tasks:

1. `pip install -r requirements.txt` succeeds in a fresh virtual environment
2. `python -c "import contracts"` imports cleanly and every fixture validates
3. `python record_serper_fixtures.py` writes `serper_fixtures.json` (needs the key; skip on reruns)
4. `python -m voice.smoke recording.wav` prints a correct transcript
5. `python -m voice.tts` writes `out_1.mp3`, `out_2.mp3`, `out_3.mp3`, each at or under 15.0 seconds by stopwatch
6. `streamlit run app/main.py` renders the full screen from fixtures, hands-free capture works, all four interface features are visible at once
7. `git status` shows `.env` untracked

**August 13 checkpoint:** post a screenshot of the full rendered screen plus your `fixtures.json` to the group, and read Austin's `mcp_server/sample_output.txt` and Ginger's `graph/sample_output.txt`. Check three things: is `price` a number where `contracts.py` says a number; do `doc_id` values look identical across all three; does any `brand` carry trailing punctuation that would break Ginger's title matching. If Austin has committed `catalog/canonical_queries.json` and your fixture products differ from his picks, reconcile now — that is what this checkpoint is for.
</verification>

<success_criteria>
- `.env.example` lists every required variable with no real secrets committed (DOC-04)
- A user records a spoken request in the browser and receives a transcript without touching a keyboard (VOICE-01)
- The transcript is displayed once transcription completes (VOICE-02)
- The spoken answer plays back as generated audio with a Play control (VOICE-03)
- The spoken answer is capped at roughly fifteen seconds, enforced in code and not only by prompt (VOICE-04)
- The comparison table shows the top three products with price, rating and product image (UI-01)
- Disagreements between private and live data are visibly flagged on the affected product row (UI-02)
- The agent step log is visible on screen during and after a run (UI-03)
- The citations panel visually distinguishes private document IDs from live source URLs (UI-04)
- `contracts.py` and `fixtures.json` are committed on day one and Austin and Ginger import from them (D-10)
- `serper_fixtures.json` is committed and Austin's `web.search` replays it without a key (D-08)
- The full screen renders from fixtures alone, with no graph and no MCP server running
</success_criteria>

<output>
Commit each task to `main` as you finish it. **Task 1 and Task 2 get committed and announced on day one** — both teammates depend on them. Post a screenshot of the rendered screen to the team on August 13.
</output>
