---
phase: 03-delivery
plan: 01
type: execute
owner: Jack
project_owner: Jack
autonomous: false
files_modified:
  - README.md
  - DEMO.md
  - presentation.pptx
  - catalog/README.md
  - mcp_server/README.md
  - graph/README.md
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-05
---

<objective>
Package the integrated system for grading. Jack owns synthesis, timing, and
final acceptance; Austin and Ginger contribute factual documentation only inside
their folders.
</objective>

<tasks>

<task owner="Austin">
  <name>Task 1 — Document data and MCP behavior</name>
  <files>catalog/README.md, mcp_server/README.md</files>
  <action>
Document CSV normalization, malformed-price handling, Parquet/index build
commands, hybrid filters, the two MCP schemas, stdio run command, cache TTL,
rate limit, request logging, fixture-key rule, retailer allowlist, and the fact
that Serper is used instead of scraping retailer pages.
  </action>
</task>

<task owner="Ginger">
  <name>Task 2 — Document graph and prompts</name>
  <files>graph/README.md</files>
  <action>
Document the Router → Planner → Retriever → Answerer/Critic flow, tool-selection
rules, material handling, safety stop, reconciliation thresholds and variant
guards, step logging, model configuration, and a table mapping every file in
`prompts/` to its consuming node or tool.
  </action>
</task>

<task owner="Jack">
  <name>Task 3 — Root README and presentation</name>
  <files>README.md, DEMO.md, presentation.pptx</files>
  <action>
Synthesize the owner documentation into a root README that covers clean-clone
setup, environment variables, build/run commands, architecture, data flow,
safety notes, limitations, recorded-Serper mode, and full-outage backup video. Include or link an architecture
diagram showing voice input → LangGraph → MCP tools → reconciliation → UI/TTS.
Add a compact retrieval-evidence table showing, for each canonical query, that
its expected doc IDs appeared at `k=5`, and link the captured catalog smoke
output in `catalog/sample_output.txt`. This uses the existing canonical set as evidence; do not build a new
evaluation harness.

Create a short presentation covering architecture, results, and limitations.
Write the exact demo sequence in `DEMO.md` and rehearse the planned content to
**390 seconds**, leaving 30 seconds of transition/error buffer before the hard
420-second limit: 40 seconds problem/data, 50 architecture, 50 private-only
budget query, 110 live conflict query, 60 results, 50 limitations, 30 wrap-up.
The budget query makes
the numeric metadata filter and no-live routing visible; the conflict query then
shows why the planner escalates and how private versus live values reconcile.
Record one successful fallback video after the live path passes.
  </action>
  <acceptance_criteria>
    - A clean clone runs by following only `README.md`
    - README covers every deliverable listed in Instructions.md lines 221–228
    - Every prompt and few-shot example is discoverable through the prompt mapping
    - README records whether every canonical query returned its expected doc IDs at `k=5` and links the captured evidence
    - The presentation covers architecture, results, and limitations, including missing private ratings
    - The timed demo shows both a private-only budget query and a live conflict query, with the planner's different source choices visible
    - The rehearsed demonstration is no more than seven minutes
    - A successful backup recording exists and uses the same canonical conflict query
  </acceptance_criteria>
</task>

</tasks>

<done>
Jack has accepted the repository, documentation, presentation, timed demo, and
backup recording as ready for submission.
</done>
